# Wazuh Search Queries

This document contains the Wazuh search queries used by SOC analysts during the Enterprise SOC Lab investigations.

The queries are organized by investigation and are intended to help analysts quickly locate, verify, and investigate Windows security events collected by Sysmon and Wazuh.

Each query has been tested in the lab and represents the common searches performed during a typical SOC L1 investigation.
---

## Investigation Whoami

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `whoami` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Whoami (Primary) | `data.win.eventdata.image:*whoami.exe` | Sysmon Event ID 1 | Finds all executions of `whoami.exe`. |
| Whoami (Alternative) | `data.win.eventdata.commandLine:whoami` | Sysmon Event ID 1 | Finds events where the command line contains `whoami`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Hostname

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `hostname` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Hostname (Primary) | `data.win.eventdata.originalFileName:"hostname.exe"` | Sysmon Event ID 1 | Finds all executions of `hostname.exe`. |
| Hostname (Alternative) | `data.win.eventdata.commandLine:"hostname"` | Sysmon Event ID 1 | Finds events where the command line contains `hostname`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Systeminfo

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `systeminfo` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Systeminfo (Primary) | `data.win.eventdata.originalFileName:"sysinfo.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `systeminfo.exe` process using the original file name. |
| Systeminfo (Alternative) | `data.win.eventdata.commandLine:"systeminfo"` | Sysmon Event ID 1 | Finds events where the command line contains `systeminfo`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Ipconfig

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `ipconfig` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Ipconfig (Primary) | `data.win.eventdata.originalFileName:"ipconfig.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `ipconfig.exe` process. |
| Ipconfig (Alternative) | `data.win.eventdata.commandLine:*ipconfig*` | Sysmon Event ID 1 | Finds events where the command line contains `ipconfig`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation ARP

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `arp` and `arp -a` commands.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| ARP (Primary) | `data.win.eventdata.originalFileName:"arp.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `arp.exe` process. |
| ARP (Alternative) | `data.win.eventdata.commandLine:*arp*` | Sysmon Event ID 1 | Finds events where the command line contains `arp` or `arp -a`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Netstat

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `netstat` and `netstat -ano` commands.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Netstat (Primary) | `data.win.eventdata.originalFileName:"netstat.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `netstat.exe` process. |
| Netstat (Alternative) | `data.win.eventdata.commandLine:*netstat*` | Sysmon Event ID 1 | Finds events where the command line contains `netstat` or `netstat -ano`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Tasklist

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `tasklist` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Tasklist (Primary) | `data.win.eventdata.originalFileName:"tasklist.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `tasklist.exe` process. |
| Tasklist (Alternative) | `data.win.eventdata.commandLine:*tasklist*` | Sysmon Event ID 1 | Finds events where the command line contains `tasklist`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Route

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `route` and `route print` commands.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Route (Primary) | `data.win.eventdata.originalFileName:"route.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `route.exe` process. |
| Route (Alternative) | `data.win.eventdata.commandLine:*route*` | Sysmon Event ID 1 | Finds events where the command line contains `route` or `route print`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Checks what other processes were executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews other events generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds other child processes created by the same parent process. |

---

## Investigation Net User

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `net user` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Net User (Primary) | `data.win.eventdata.originalFileName:"net.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `net.exe` process. |
| Net User (Alternative) | `data.win.eventdata.commandLine:*net user*` | Sysmon Event ID 1 | Finds executions where the command line contains `net user`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Reviews other processes executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews activity from the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds child processes created by the same parent process. |

---

## Investigation Net LocalGroup

The following Wazuh search queries are commonly used by a **SOC L1 Analyst** to investigate the execution of the Windows `net localgroup` command.

| Investigation | Search Query | Event ID | Purpose |
|---------------|--------------|----------|---------|
| Net LocalGroup (Primary) | `data.win.eventdata.originalFileName:"net.exe"` | Sysmon Event ID 1 | Finds all executions of the Windows `net.exe` process. |
| Net LocalGroup (Alternative) | `data.win.eventdata.commandLine:*net localgroup*` | Sysmon Event ID 1 | Finds executions where the command line contains `net localgroup`. |
| Process Create Events | `data.win.system.eventID:1` | Sysmon Event ID 1 | Displays all Sysmon Process Create events for timeline analysis. |
| Same User Activity | `data.win.eventdata.user:<Username>` | Sysmon Event ID 1 | Reviews other processes executed by the same user. |
| Same Endpoint Activity | `agent.name:<Agent_Name>` | Sysmon Event ID 1 | Reviews activity generated by the same endpoint. |
| Same Parent Process | `data.win.eventdata.parentProcessId:<Parent_Process_ID>` | Sysmon Event ID 1 | Finds child processes created by the same parent process. |