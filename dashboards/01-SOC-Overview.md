# Dashboard 01 - SOC Overview

## Overview

The **Enterprise SOC - Overview** dashboard provides SOC analysts with a centralized, high-level view of the overall security posture of the Enterprise SOC Lab.

Instead of navigating multiple Wazuh modules, analysts can quickly monitor alert volume, alert severity, endpoint availability, and overall security activity from a single dashboard. This dashboard serves as the primary entry point for SOC monitoring, enabling analysts to rapidly assess the health of the environment before moving to specialized dashboards for endpoint monitoring, threat detection, MITRE ATT&CK analysis, and detection analytics.

## Objective

- Provide a centralized overview of the Enterprise SOC environment.
- Monitor overall alert volume and severity in real time.
- Display endpoint availability and security activity trends.
- Enable rapid assessment of the organization's security posture.
- Serve as the primary entry point for security monitoring and incident analysis.

---

## Dashboard Information

| Property | Value |
|----------|-------|
| Dashboard Name | Enterprise SOC - Overview |
| Platform | Wazuh Dashboard |
| Visualization Engine | OpenSearch Dashboards |
| Data Source | wazuh-alerts-* | wazuh-monitoring-* |
| Time Filter | Last 24 Hours |

---

### Step 1 - Navigate to Dashboard

Open the Wazuh Dashboard and navigate to:

Menu → Explore → Dashboards

This is the starting point for creating custom SOC dashboards.

Image: #1

Path:
dashboards/screenshots/dashboard-01/01-dashboard-home.png

### Step 2 - Create Dashboard

Click **Create New Dashboard**.

A blank dashboard is created where visualizations can be added later.

Image: #2

Path:
dashboards/screenshots/dashboard-01/02-create-dashboard.png

### Step 3 - Create Visualization

Click **Create new dashboard**.

Select the **Metric** visualization.

Metric visualizations are commonly used to display single-value KPIs such as:

- Total Alerts
- Critical Alerts
- Active Agents
- Events Today

Image: #3

Path:
dashboards/screenshots/dashboard-01/03-select-visualization-type.png


### Step 4 - Select Data Source

Choose the following index pattern:

wazuh-alerts-*

This index contains all alerts generated by the Wazuh Rule Engine and is the primary data source for SOC monitoring dashboards.

Image: #4

Path:
dashboards/screenshots/dashboard-01/04-select-data-source.dashboard-01/

### Step 5 - Configure Metric

Configuration

Visualization Type:
Metric

Aggregation:
Count

Index Pattern:
wazuh-alerts-*

The Count aggregation calculates the total number of alert documents within the selected time range.

Image: #5

Path:
dashboards/screenshots/dashboard-01/05-total-alerts-metric.png

### Step 6 - Save Visualization

Save the visualization using the following settings.

Title:
SOC - Total Alerts

Description:
Displays the total number of alerts generated in the selected time range.

Enable:
Add to Dashboards after saving

Image: #6

Path:
dashboards/screenshots/dashboard-01/06-save-visualization.png

### Step 7 - Verify Visualization

After saving, verify that the visualization appears in the Visualization Library.

This confirms that the visualization has been successfully created and is ready to be reused in any dashboard.

Image: #7

Path:
dashboards/screenshots/dashboard-01/07-visualization-library.png

### Step 8 - Verify Dashboard

Return to the Dashboard page and verify that the Enterprise SOC - Overview dashboard has been successfully created.

Image: #8

Path:
dashboards/screenshots/dashboard-01/08-dashboard-library.png


## Step 9 - Add the Total Alerts Widget to the Dashboard

After verifying that the **SOC - Total Alerts** visualization has been successfully saved, add it to the **Enterprise SOC - Overview** dashboard.

Navigate to:

```text
Menu
    └── Explore
          └── Dashboards
```

Open:

```text
Enterprise SOC - Overview
```

Click **Edit**, then select **Add**.

The **Add Panels** window opens, displaying all saved visualizations.

Select the **SOC - Total Alerts** visualization and click **Save** to add the widget to the dashboard.

### Screenshot

**Image #09**

```text
dashboards/screenshots/dashboard-01/09-add-total-alerts-widget.png
```

## Step 10 - Save the Enterprise SOC Dashboard

Verify that the **SOC - Total Alerts** widget has been successfully added to the **Enterprise SOC - Overview** dashboard.

Finally, click **Save** in the **top-right corner** to save the updated dashboard.

The newly created visualization is also available in the **Visualize** section and can be reused in other dashboards.

Navigate to:

```text
Explore
    └── Visualize
```
## Step 10 - Save the Enterprise SOC Dashboard

Verify that the **SOC - Total Alerts** widget has been successfully added to the **Enterprise SOC - Overview** dashboard.

Finally, click **Save** in the **top-right corner** to save the updated dashboard.

The newly created visualization is also available in the **Visualize** section and can be reused in other dashboards.

Navigate to:

```text
Explore
    └── Visualize
```

### Screenshot

**Image #10**

```text
dashboards/screenshots/dashboard-01/10-updated-enterprise-soc-visualization.png
```


# Widget 02 - Critical Alerts

## Objective

The **Critical Alerts** widget displays the total number of critical security alerts generated by the Wazuh Rule Engine during the selected time range.

This widget counts only alerts with **Rule Level 15 or higher**, helping SOC analysts quickly identify high-priority security incidents.

---

## Step 1 - Verify the Default Critical Alert Filter

Before creating the widget, verify the default rule level used by the Wazuh Overview dashboard.

Navigate to:

**Overview → Critical Severity**

Click the **Critical Severity** card to open the alert details.

> Verify that the data source is **wazuh-alerts-\*** (not **wazuh-archives-\***), since alert widgets should use the alerts index.

Also verify that the filter is configured as:

```
rule.level: 15 to Infinity
```

This confirms the correct severity range used by Wazuh for Critical Alerts.

### Screenshot

**Image #01**

```
dashboards/screenshots/dashboard-01/11-dashboard-overview.png
```

---

### Screenshot

**Image #02**

```
dashboards/screenshots/dashboard-01/12-verify-critical-rule-level.png
```

---

## Step 2 - Open the Dashboards Module

Click the navigation menu (☰) in the top-left corner.

Navigate to:

```
Explore
    └── Dashboards
```

### Screenshot

**Image #03**

```
dashboards/screenshots/dashboard-01/13-open-navigation-menu.png
```

---

## Step 3 - Open the Enterprise SOC Dashboard

Open the previously created dashboard.

```
Enterprise SOC - Overview
```

This dashboard will contain all custom SOC monitoring widgets.

---

## Step 4 - Open the Dashboard in Edit Mode

Open the previously created **Enterprise SOC - Overview** dashboard.

Since the dashboard is currently empty, click the **Edit** button located in the upper-right corner to enable editing mode.

Editing mode allows new visualizations, panels, and widgets to be added to the dashboard.

### Screenshot

**Image #05**

Path:

```text
dashboards/screenshots/dashboard-01/14-edit-enterprise-soc-dashboard.png
```

## Step 5 - Add a Dashboard Panel

While the dashboard is in Edit mode, click **Add** from the top navigation bar.

The **Add Panels** window will open, displaying all saved visualizations available for reuse.

### Screenshot

**Image #06**

Path:

```text
dashboards/screenshots/dashboard-01/15-open-add-panel.png
```

## Step 6 - Create a New Visualization

In the **Add Panels** window, click **Create new**.

From the available options, select:

```text
Visualization
```

This option opens the Visualization editor, where a new widget can be created and configured before adding it to the dashboard.

### Screenshot

**Image #06**

Path:

```text
dashboards/screenshots/dashboard-01/16-select-visualization.png
```

## Step 7 - Select the Metric Visualization

The **New Visualization** window will open, displaying all available visualization types.

Select the **Metric** visualization.

The Metric visualization is used to display a single numerical value, making it ideal for showing the total number of alerts for a specific severity level.

### Screenshot

**Image #07**

Path:

```text
dashboards/screenshots/dashboard-01/17-select-metric-visualization.png
```

## Step 8 - Select the Data Source

After selecting the **Metric** visualization, the **New Metric / Choose a source** window will appear.

From the available data sources, select **wazuh-alerts-\***.

> **Note**
>
> Use **wazuh-alerts-\*** because this index contains alert events generated by the Wazuh Rule Engine. Do **not** select **wazuh-archives-\***, as it stores all collected events rather than only security alerts.

### Screenshot

**Image #08**

Path:

```text
dashboards/screenshots/dashboard-01/18-select-alert-data-source.png
```

## Step 9 - Add Alert Filters

The **Metric** visualization is now created, but it currently displays the total number of alerts from the **wazuh-alerts-*** index.

To display only **Critical** alerts, apply the required filters.

1. Click **Add filter**.
2. Add the following filters:

   - **Field:** `manager.name`
     - **Operator:** `is`
     - **Value:** `wazuh-server`

   - **Field:** `rule.level`
     - **Operator:** `is between`
     - **Value:** `15` to `Infinity`

After applying these filters, the metric will display only **Critical severity alerts**.

> **Note**
>
> The `manager.name` filter limits the results to alerts generated by the **Wazuh Manager**, while the `rule.level` filter ensures that only **Critical alerts (Level 15+)** are counted.

### Screenshot

**Image #09 – Add Filters for Critical Alerts**

```text
dashboards/screenshots/dashboard-01/19-add-critical-filters.png
```

## Step 10 - Save the Visualization

After configuring the required filters, click the **Save** button located in the **top-right corner** of the Visualization page.

The **Save Visualization** dialog will appear.

Provide the following information:

**Title**

```text
SOC - Critical Alerts
```

**Description**

```text
Displays all critical security alerts (Rule Level 15+) generated by the Wazuh Rule Engine within the selected time range.
```

### Screenshot

**Image #12 – **Updated Enterprise SOC Dashoboard**

```text
dashboards/screenshots/dashboard-01/20-save-critical-visualization.png
```

## Step 11 - Save the Enterprise SOC Dashboard

After clicking **Save and return**, the **SOC - Critical Alerts** visualization is automatically added to the **Enterprise SOC - Overview** dashboard.

Review the widget to ensure it displays the correct number of **Critical severity alerts (Rule Level 15+)**.

Finally, click **Save** in the top-right corner to save the updated dashboard.


### Screenshot

**Image #12 – **Updated Enterprise SOC Dashoboard**

```text
dashboards/screenshots/dashboard-01/21-Updated-SOC-dashboard.png
```

> **Result**
>
> The **SOC - Critical Alerts** widget is now successfully added to the **Enterprise SOC - Overview** dashboard and will display the total number of Critical alerts based on the selected time range.

### Screenshot

**Image #13 – Critical Alert Widget Added to Dashboard**

```text
dashboards/screenshots/dashboard-01/22-critical-widget-added-dashboard.png
```

## Create High, Medium, and Low Alert Widgets

The process for creating the **High**, **Medium**, and **Low** alert widgets is identical to the **Critical** alert widget created in the previous section.

Since the same workflow is repeated, the step-by-step instructions and screenshots are not included again.

> **Note**
>
> In this lab, the visualizations were created directly from the **Enterprise SOC - Overview** dashboard using **Edit → Add → Create new → Visualization**.
>
> Although visualizations can also be created from the **Visualize** section first and then added to a dashboard, creating them directly from the dashboard keeps the workflow simpler and avoids switching between different pages.

Create the remaining widgets by following the same steps used for the **Critical** alert widget, changing only the alert severity filter, visualization title, and description.

| Severity | Rule Level | Visualization Title |
|----------|------------|---------------------|
| High | 12 – 14 | `SOC - High Alerts` |
| Medium | 7 – 11 | `SOC - Medium Alerts` |
| Low | 0 – 6 | `SOC - Low Alerts` |

The dashboard will now contain separate widgets for **Critical**, **High**, **Medium**, and **Low** alerts, providing a quick overview of alert severity distribution.

## Final Dashboard Overview

After creating the Critical, High, Medium, and Low alert widgets, the Enterprise SOC - Overview dashboard provides a consolidated view of alert severity levels.

This allows SOC analysts to quickly monitor the number of alerts across each severity category from a single dashboard.

### Screenshot

**Image #14 – Enterprise SOC - Overview Dashboard**

```text
dashboards/screenshots/dashboard-01/23-enterprise-soc-overview-dashboard.png
```
---

# Widget 03 - Active Agents

## Objective

The **Active Agents** widget displays the total number of Wazuh agents that are currently active and communicating with the Wazuh Manager.

This widget helps SOC analysts quickly verify endpoint availability and ensure that monitored systems are actively reporting security events.

---

### Step 1 - Verify the Default Agent Summary

Before creating the visualization, verify the default **Agents Summary** widget provided by Wazuh.

Navigate to:

```text
Overview
```

Locate the **Agents Summary** widget and verify that it displays the current agent status, including:

- Active
- Disconnected

This widget will be used as the reference for creating the custom **Active Agents** visualization in the Enterprise SOC Dashboard.

---

## Step 2 - Verify the Agent Status

Click the **Agents Summary** widget.

The **Endpoints → Agents** page opens, displaying detailed information about all enrolled Wazuh agents.

Verify that the following agent status information is available:

- Active
- Disconnected
- Pending
- Never connected

Also verify that the required monitoring information is available, including the agent **ID**, **Name**, **IP Address**, and **Status**.

This confirms that the required monitoring data is available before creating the visualization.

### Screenshot

**Image #02**

```text
dashboards/screenshots/dashboard-01/24-open-agent-details.png
```

---

## Step 3 - Open the Enterprise SOC Dashboard

From the Wazuh Dashboard, navigate to the custom dashboard created for this project.

Open:

```text
Enterprise SOC - Overview
```

This dashboard will be used to create and manage custom SOC monitoring widgets.

---

## Step 4 - Open the Dashboard in Edit Mode

Open the previously created **Enterprise SOC - Overview** dashboard.

Click the **Edit** button located in the **top-right corner** of the dashboard to enable editing mode.

Editing mode allows new visualizations and widgets to be added to the dashboard.

---

## Step 5 - Add a Dashboard Panel

While the dashboard is in **Edit** mode, click **Add** from the top navigation bar.

The **Add Panels** window opens, displaying all previously saved visualizations that can be added to the dashboard.

---

## Step 6 - Create a New Visualization

In the **Add Panels** window, click **Create new**.

From the available options, select:

```text
Visualization
```

This opens the Visualization editor, where a new widget can be created and configured.

---

## Step 7 - Select the Metric Visualization

The **New Visualization** window opens, displaying all available visualization types.

Select the **Metric** visualization.

A Metric visualization is designed to display a single numerical value, making it suitable for showing the total number of active Wazuh agents.

---

## Step 8 - Select the Data Source

After selecting the **Metric** visualization, the **Choose a source** window appears.

Select the following data source:

```text
wazuh-monitoring-*
```

> **Note**
>
> Use **wazuh-monitoring-*** because this index stores agent monitoring information such as agent status, heartbeat, connectivity, and health data. It is the appropriate data source for creating the **Active Agents** widget.

### Screenshot

**Image #07**

```text
dashboards/screenshots/dashboard-01/25-select-monitoring-data-source.png
```

---

## Step 9 - Configure the Active Agents Metric

The **Metric** visualization is now created, but it currently displays all monitoring records from the **wazuh-monitoring-*** index.

To display only the total number of **active Wazuh agents**, configure the metric as follows.

### Metric Aggregation

Select:

- **Aggregation:** `Unique Count`
- **Field:** `id`

> **Note**
>
> Do **not** use the **Count** aggregation.
>
> The `wazuh-monitoring-*` index stores multiple monitoring records (heartbeats) for the same agent. Using **Count** would count every monitoring record instead of the actual number of agents.
>
> Using **Unique Count** on the **id** field ensures that each Wazuh agent is counted only once, providing an accurate total of active agents.

Next, click **Add filter** and configure the following filter:

| Field | Operator | Value |
|--------|----------|-------|
| `status` | `is` | `active` |

After applying the filter, the visualization displays only agents that are currently active and communicating with the Wazuh Manager.

### Screenshot

**Image #08**

```text
dashboards/screenshots/dashboard-01/26-configure-active-agents-status.png
dashboards/screenshots/dashboard-01/27-configure-active-agents-metric.png
```

---

## Step 10 - Save the Visualization

After configuring the metric, click the **Save** button located in the **top-right corner** of the Visualization page.

The **Save Visualization** dialog will appear.

Provide the following information:

**Title**

```text
SOC - Active Agents
```

**Description**

```text
Displays the total number of unique Wazuh agents that are currently active and communicating with the Wazuh Manager.
```

Click **Save and return** to store the visualization in the Visualization Library.


> **Note**
>
> Before click on **Save and return** enable **Add to Dashboards after saving**.

### Screenshot

**Image #09**

```text
dashboards/screenshots/dashboard-01/28-save-active-agents-visualization.png
```

---

## Step 11 - Save the Enterprise SOC Dashboard

After clicking **Save and return**, the **SOC - Active Agents** visualization is automatically added to the **Enterprise SOC - Overview** dashboard.

Review the widget to ensure it displays the correct number of active Wazuh agents.

Finally, click **Save** in the **top-right corner** to save the updated dashboard.

```md
The newly created visualization is also available in the **Visualize** section and can be reused in other dashboards.

Navigate to:

```text
Explore
    └── Visualize

### Screenshot

**Image #10**

```text
dashboards/screenshots/dashboard-01/29-updated-active-agents-dashboard.png
```

---

> **Result**
>
> The **SOC - Active Agents** widget is now successfully added to the **Enterprise SOC - Overview** dashboard and displays the total number of unique Wazuh agents that are currently active and communicating with the Wazuh Manager.


# Widget 04 - Alerts Over Time

## Objective

The **Alerts Over Time** widget displays the number of security alerts generated during the selected time range.

This visualization helps SOC analysts identify alert spikes, monitor alert trends, and determine when suspicious activity occurred.

---

## Step 1 - Open the Enterprise SOC Dashboard

From the Wazuh Dashboard, navigate to the custom dashboard created for this project.

Open:

```text
Enterprise SOC - Overview
```

This dashboard will be used to create and manage custom SOC monitoring widgets.

---

## Step 2 - Open the Dashboard in Edit Mode

Open the previously created **Enterprise SOC - Overview** dashboard.

Click the **Edit** button located in the **top-right corner** of the dashboard to enable editing mode.

Editing mode allows new visualizations and widgets to be added to the dashboard.

---

## Step 3 - Add a Dashboard Panel

While the dashboard is in **Edit** mode, click **Add** from the top navigation bar.

The **Add Panels** window opens, displaying all previously saved visualizations available for reuse.

---

## Step 4 - Create a New Visualization

In the **Add Panels** window, click **Create new**.

From the available options, select:

```text
Visualization
```

This opens the Visualization editor, where a new widget can be created and configured.

---

## Step 5 - Select the Line Visualization

The **New Visualization** window opens, displaying all available visualization types.

Select the **Line** visualization.

A Line visualization is ideal for displaying alert trends over time, allowing SOC analysts to quickly identify spikes and changes in alert activity.

### Screenshot

**Image #05**

```text
dashboards/screenshots/dashboard-01/30-select-line-visualization.png
```

---

## Step 6 - Select the Data Source

After selecting the **Line** visualization, the **Choose a source** window appears.

Select the following data source:

```text
wazuh-alerts-*
```

> **Note**
>
> Use **wazuh-alerts-*** because this index contains security alerts generated by the Wazuh Rule Engine. These alerts are used to visualize alert activity over time.


## Step 7 - Configure the Alert Trend

The Line visualization is now created, but it currently displays all alert documents without grouping them by time.

Configure the visualization using the following settings:

### Y-Axis

- **Aggregation:** `Count`

### X-Axis

- **Aggregation:** `Date Histogram`
- **Field:** `@timestamp`
- **Minimum Interval:** `Auto`
- **Drop partial buckets** `OFF`

The **Count** aggregation calculates the total number of alerts, while the **Date Histogram** groups those alerts based on their timestamp.

This configuration produces a time-based visualization that displays how alert activity changes throughout the selected time range.

### Screenshot

**Image #07**

```text
dashboards/screenshots/dashboard-01/31-configure-alerts-over-time.png
```

## Step 8 - Save the Visualization

After configuring the visualization, click the **Save** button located in the **top-right corner** of the page.

The **Save Visualization** dialog will appear.

Provide the following information:

**Title**

```text
SOC - Alerts Over Time
```

**Description**

```text
Displays the number of security alerts generated over time, helping SOC analysts identify alert trends, activity spikes, and unusual patterns within the selected time range.
```

Click **Save and return** to store the visualization in the Visualization Library.

> **Note**
>
> Before clicking **Save and return**, enable **Add to Dashboards after saving**.

### Screenshot

**Image #08**

```text
dashboards/screenshots/dashboard-01/32-save-alerts-over-time-visualization.png
```

---

## Step 9 - Save the Enterprise SOC Dashboard

After clicking **Save and return**, the **SOC - Alerts Over Time** visualization is automatically added to the **Enterprise SOC - Overview** dashboard.

Review the visualization to ensure that the alert trend is displayed correctly across the selected time range.

Finally, click **Save** in the **top-right corner** to save the updated dashboard.

The newly created visualization is also available in the **Visualize** section and can be reused in other dashboards.

Navigate to:

```text
Explore
    └── Visualize
```

### Screenshot

**Image #09**

```text
dashboards/screenshots/dashboard-01/33-updated-alerts-over-time-dashboard.png
```

---

> **Result**
>
> The **SOC - Alerts Over Time** widget is now successfully added to the **Enterprise SOC - Overview** dashboard and provides a time-based view of alert activity, enabling SOC analysts to quickly identify trends, spikes, and periods of increased security events.


## Final Dashboard Documentation

### Dashboard Layout Recommendation

The **Enterprise SOC - Overview** dashboard is designed to provide SOC analysts with a high-level overview of the security posture of the monitored environment.

The dashboard prioritizes key security metrics and alert trends, enabling analysts to quickly assess the current state of the SOC before performing detailed investigations in dedicated dashboards.

### Dashboard Layout

**First Row – Key Security Metrics**

- Total Alerts
- Critical Alerts
- High Alerts
- Medium Alerts
- Low Alerts
- Active Agents

These metric widgets provide an immediate overview of the total alert volume, alert severity distribution, and the operational status of monitored endpoints.

---

**Second Row – Alert Trends**

- Alerts Over Time

This visualization displays the trend of security alerts over the selected time range, allowing SOC analysts to identify spikes in activity, monitor changes in the alert volume, and recognize periods of increased security events.

---

The dashboard layout focuses on providing a concise operational overview without overwhelming the analyst with detailed detection or threat intelligence information. More detailed analysis is available in the dedicated **Threat Detection**, **MITRE ATT&CK Analysis**, and **Detection Analytics** dashboards.

## Final Enterprise SOC - Overview Dashboard

The completed **Enterprise SOC - Overview** dashboard serves as the primary entry point for monitoring the Enterprise SOC Lab.

It provides SOC analysts with real-time visibility into overall alert volume, alert severity, endpoint availability, and alert activity trends. This high-level operational view enables analysts to quickly evaluate the security posture of the environment before proceeding to specialized dashboards for threat analysis, endpoint monitoring, and incident investigation.

### Screenshot

**Final Enterprise SOC - Overview Dashboard**

```text
dashboards/screenshots/dashboard-01/34-final-enterprise-soc-overview-dashboard.png
```